"""
`arklight.backend.desktop` -- template/runtime content for `arklight
desktop scaffold`. See `arklight.backend.desktop.runtime` and
`arklight.cli.desktop` (the CLI-facing counterpart that owns
filesystem writes, mirroring the Android backend's own split).
"""

from __future__ import annotations
