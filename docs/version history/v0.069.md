# v0.069 -- JS vocabulary: cross-language formatting/case batteries + `Provider`, stage 5/6

**Status: DONE.** Milestone closed at package version `0.069`. Both pieces
shipped: JS vocabulary stage 9/10 (`0.06615`) and `Provider` stage 5/6 (`0.06614`).
Stage 10 of the JS ladder and `Provider` stage 6 remain PLANNED for `v0.070`. Two independent, accepted
pieces of work share this milestone slot:

1. Forward-looking summary, written ahead of implementation as part of
   staging
   [`docs/Implementation/JS-VOCABULARY-ADDENDUM-v0.070.md`](../Implementation/JS-VOCABULARY-ADDENDUM-v0.070.md)
   (stage 9 of 10).
2. `Provider`, stage 5 of 6, documented in
   [`docs/Foundational/PROVIDER-SDK.md`](../Foundational/PROVIDER-SDK.md).

Both sections below will be updated to reflect actual shipped
behavior once this version lands.

## JS vocabulary, stage 9 of 10

**Shipped** as `0.06615`. The section below reflects actual behavior.

Ninth rung -- formatting and case-conversion idioms common across
Python/Rust "humanize" and inflection libraries (most tracing back to
Ruby on Rails' inflector), none of which JS ships natively.

### What shipped

- `to_ordinal` -- `1` -> `"1st"`, `2` -> `"2nd"`, etc.
- `humanize_bytes` -- `1536` -> `"1.5 KB"`.
- `humanize_duration` -- seconds -> a `"2h 15m"`-style string.
- `to_snake_case`, `to_camel_case`, `to_kebab_case`, `to_title_case`
  -- pure string-rewrite case converters, no irregular-word problem,
  so despite living in this cross-language batch they're exactly as
  cheap as the earlier catalog stages.

## `Provider`, stage 5 of 6: `arklight search` integration

**Shipped** as `0.06614` (out-of-band, ahead of the rest of this slot).
The section below reflects actual behavior.

Extends `arklight search`'s existing schema-lookup reflection to a
registered `Provider`'s capability contract, the same "look something
up without opening a file" convenience `SCHEMA` already gets for
components.

### What shipped

- `arklight search <provider-name>` prints the declared capability
  contract (label and capabilities, in declared order) via an exact,
  case-insensitive lookup. `Site(provider=...)` registers the declaration
  in `arklight.provider.PROVIDER_REGISTRY`; there is no typo-suggestion
  path for provider labels (a label is a free string). A component or
  closed-vocabulary name always wins a collision.
- **Limitation:** `arklight search` never loads a project's site file,
  so a provider is only findable in a process that has constructed its
  `Site` (the same limitation user-defined components already have).

