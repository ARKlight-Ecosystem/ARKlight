# v0.062 -- JS vocabulary: string casing + comparison predicates

**Status: PLANNED -- not yet started.** Forward-looking summary,
written ahead of implementation as part of staging
[`docs/Implementation/JS-VOCABULARY-ADDENDUM-v0.070.md`](../Implementation/JS-VOCABULARY-ADDENDUM-v0.070.md)
(stage 2 of 10). Will be updated to reflect actual shipped behavior
once this stage lands.

Second rung -- the last of the genuinely one-hour additions, plus the
`Show` predicates that were already speced but never wired up.

## What's planned

- `Derive.uppercase`, `Derive.trim` -- siblings of
  `Derive.join`/`Derive.format`.
- `Predicate.equals`, `Predicate.gt`, `Predicate.lt` -- already
  speced alongside `Derive.compare`'s `eq/ne/gt/lt/gte/lte` op set,
  just never wired into `PREDICATE_REGISTRY` (today only
  `truthy`/`falsy`).
