# v0.070 -- JS vocabulary: capstone (pluralize + random_int) + `Provider`, stage 6/6

**Status: PLANNED -- not yet started.** Two independent, accepted
pieces of work share this milestone slot, each reaching its own
capstone here:

1. Forward-looking summary, written ahead of implementation as part of
   staging
   [`docs/Implementation/JS-VOCABULARY-ADDENDUM-v0.070.md`](../Implementation/JS-VOCABULARY-ADDENDUM-v0.070.md)
   (stage 10 of 10, the ladder's namesake).
2. `Provider`, stage 6 of 6 (capstone), staged in
   [`docs/Implementation/PROVIDER-SDK-ADDENDUM.md`](../Implementation/PROVIDER-SDK-ADDENDUM.md).

Both sections below will be updated to reflect actual shipped
behavior once this version lands.

## JS vocabulary, stage 10 of 10 (capstone)

Tenth and final rung -- the two entries flagged throughout this
ladder as needing an explicit design exception before they ship,
placed last so review attention concentrates on them rather than
being spread across the whole catalog.

### What's planned

- **`pluralize`** -- `"item"` + count -> `"item"`/`"items"`,
  ubiquitous in list-count UI copy. English pluralization has enough
  irregular forms (`person` -> `people`, `octopus` -> `octopi`) that
  this ships with a small irregular-noun table plus a documented
  regular-plural-only fallback (`+s`/`+es`), not a claim of
  exhaustive coverage.
- **`random_int`** -- unlike every other derivation added across
  `v0.061`-`v0.069`, this is not a pure function of its inputs, which
  cuts against the "server-rendered `Bind` text agrees with the
  client recompute" invariant `sum.py`'s own docstring calls out.
  Ships with an explicit, documented exception: excluded from
  build-time pre-rendering entirely, resolved client-side only.

This closes the JS vocabulary addendum -- everything philosophy-
compliant from
[`docs/Proposals/JS-VOCABULARY-EXPANSION-PROPOSAL.md`](../Proposals/JS-VOCABULARY-EXPANSION-PROPOSAL.md)'s
Tier 1, Tier 2, and §6 is now staged somewhere in `v0.061`-`v0.070`.
The proposal's Tier 3 (client-side data fetch, reorderable lists,
sort/filter, file-upload preview) and Tier 4 (out of scope) remain
open, unaccepted proposals -- neither needs "more vocabulary," each
needs a new IR node or a philosophy exception this addendum
deliberately doesn't grant.

## `Provider`, stage 6 of 6 (capstone): capability enum finalization

Locks down the closed capability vocabulary stage 1 first declared
against (§7.2 of the proposal), now informed by the concrete shape
stages 2-5 actually needed, and documents the full landed contract.

### What's planned

- A fixed, versioned capability enum (e.g. `auth`, `read`, `write`,
  `subscribe`) replacing whatever provisional set stage 1 shipped
  with, per the same closed-registry discipline `Action.*`/
  `Behavior.*` already use everywhere else in the runtime.
- `docs/Foundational/EXPERIMENTAL-APIS.md` and root `README.md`
  updated to document the full `Provider` contract as a shipped
  experimental feature.

This closes the `Provider` ladder -- an interface only, no vendor
code, no networking code in ARKlight's core, exactly as scoped in
[`docs/Proposals/PROVIDER-SDK-PROPOSAL.md`](../Proposals/PROVIDER-SDK-PROPOSAL.md).
A concrete integration (a real Firebase provider, a hand-rolled API
client) remains something a site author or a separate, non-core
package writes against the contract -- not something this project
ships or maintains.

